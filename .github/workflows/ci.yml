name: "CI"
on:
    pull_request:
env:
    USER: runner
    DEV_SHELL_NAME: CI
jobs:
    tests:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2.4.0
              with:
                  fetch-depth: 0
            - name: "Setup env"
              uses: JRMurr/direnv-nix-action@1257f8a701761e978c612852b28f94389c809030
            - name: install node modules
              run: npm ci
              shell: bash
            - name: type check
              run: just type-check
              shell: bash
